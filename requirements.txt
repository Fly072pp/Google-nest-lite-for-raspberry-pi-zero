# ─────────────────────────────────────────────────────────────────────────────
# Dépendances — Assistant vocal Edge AI (Raspberry Pi Zero 2W)
# ─────────────────────────────────────────────────────────────────────────────

# Audio & Math
pyaudio>=0.2.14
numpy>=1.26.0
scipy>=1.10.0
scikit-learn>=1.3.0
tqdm>=4.66.0

# Détection du mot d'éveil (Géré manuellement dans install.sh)

# Speech-to-Text
faster-whisper>=1.0.0       # pip install faster-whisper

# LLM via API (mode "api")
openai>=1.0.0

# TTS
pyttsx3>=2.90             # fallback universel (espeak sur Linux)
# piper-tts est installé manuellement (binaire + modèle .onnx)
# Voir : https://github.com/rhasspy/piper/releases

# Monitoring RAM (optionnel mais recommandé)
psutil>=5.9.0
pexpect>=4.9.0

# API locale Somfy
requests>=2.28.0

# Interface d'Administration Web
Flask>=2.0.0

# Chromecast Control
pychromecast>=14.0.0

# Wake on LAN
wakeonlan>=3.1.0
